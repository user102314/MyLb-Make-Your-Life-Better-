# Configuration G√©n√©rale
server.address=0.0.0.0
server.port=9090
# application.properties
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
management.endpoints.web.exposure.include=mappings
spring.security.enabled=false
logging.level.org.springframework.web.servlet.mvc.method.annotation=TRACE

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
# ----------------------------------------------------
# 1. Configuration de la Base de Donn√©es
# ----------------------------------------------------
spring.jpa.hibernate.ddl-auto=update
# CORRECTION D'URL: Retrait des caract√®res invalides
spring.datasource.url=jdbc:mysql://localhost:3306/mylbdb?serverTimezone=UTC
spring.datasource.username=root
# Laissez ceci vide si votre utilisateur 'root' n'a pas de mot de passe
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ----------------------------------------------------
# 2. Configuration du Serveur Gmail SMTP
# ----------------------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mylbmakeyoulifebetter@gmail.com

spring.mail.password=rafz anxq sqjl defn

# ----------------------------------------------------
# 3. Propri√©t√©s de S√©curit√© et Encodage
# ----------------------------------------------------
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.sender-address=${spring.mail.username}
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.debug=true

# Force l'encodage de tous les fichiers de l'application
spring.mandatory-file-encoding=UTF-8

# DÈfinit l'encodage par dÈfaut du JavaMailSender ‡ UTF-8
spring.mail.properties.mail.mime.charset=UTF-8

# Fichier: src/main/resources/application.properties

# Taille maximale du fichier (par exemple, 10 MÈgaoctets)
spring.servlet.multipart.max-file-size=10MB

# Taille maximale de la requÍte totale (lÈgËrement supÈrieure ‡ la taille du fichier)
spring.servlet.multipart.max-request-size=11MB